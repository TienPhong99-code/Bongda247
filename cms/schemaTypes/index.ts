import category from './category'
import post from './post' // Nếu bạn đã tạo file post.ts trước đó
import matchInsight from './matchInsight' // Nếu bạn đã tạo file matchInsight.ts trước đó

export const schemaTypes = [category, post, matchInsight]
